# 跌倒检测系统 Python 依赖
# Windows 版
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
bleak>=0.21.0
websockets>=12.0

# Raspberry Pi 5 版 (额外依赖)
# picamera2: sudo apt install python3-picamera2
# dbus-next: pip install dbus-next
# bluetooth: sudo apt install bluetooth bluez
